{
  "name": "esferas-do-poder",
  "version": "0.1.0",
  "private": true,
  "description": "Dev tooling for the Esferas do Poder webapp. The browser app itself ships zero dependencies and needs no build — this package.json exists only for offline scripts (data migration, validation, type-checking). Node tooling is CommonJS to match the existing parser.js/split-source.js it reuses.",
  "scripts": {
    "extract": "node scripts/extract-structured.js",
    "validate": "node scripts/validate.js",
    "test": "node scripts/test-rules.js",
    "typecheck": "tsc --noEmit",
    "emulators": "npx -y firebase-tools emulators:start --only auth,firestore",
    "rules:deploy": "npx -y firebase-tools deploy --only firestore:rules"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "ajv": "^8.17.1",
    "jsdom": "^29.1.1",
    "typescript": "^5.6.3"
  }
}
